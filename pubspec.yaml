name: flutter_app_dialog
description: Flutter app dialog is a simple package to build a custom dialog
version: 1.0.0
homepage: https://github.com/Nhattandev/flutter_app_dialog.git

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  flutter_screenutil: ^4.0.3+3

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: